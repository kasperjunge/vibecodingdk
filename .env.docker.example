# =============================================================================
# DOCKER COMPOSE ENVIRONMENT
# =============================================================================
# For running with Docker Compose
# Copy this file to .env.docker and update with your actual values

ENVIRONMENT=prod

# Domain Configuration (for production CORS and URL generation)
DOMAIN=yourdomain.com
USE_SSL=true
ALLOWED_ORIGINS=https://yourdomain.com

# Database Configuration
POSTGRES_DB=vibecodingdk
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_secure_password

# Security
SECRET_KEY=changeme_secure_secret_key

# Admin User
ADMIN_EMAIL=admin@example.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme_admin_password
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User

# Email Configuration
# Options: mailhog (for development), gmail, resend (for production)
EMAIL_PROVIDER=resend
MAIL_FROM=noreply@yourdomain.com
MAIL_FROM_NAME=Vibe Coding DK

# For Resend (production)
RESEND_API_KEY=your_resend_api_key_here

# For Gmail (alternative)
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_STARTTLS=true

# Slack Configuration (optional)
SLACK_WEBHOOK_URL=

# Verification Settings
VERIFICATION_TOKEN_EXPIRE_HOURS=24
